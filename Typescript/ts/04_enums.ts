 // Regular enum
enum Colors {
    Red = "red",
    Green = "green",
    Blue = "blue"
}

const alexTShrtColor: Colors = Colors.Green;
// console.log("alex", alexTShrtColor);

// Const enum
const enum Size {
    s = 10,
    m = 7,
    xl = 3
}

const size: Size = Size.xl;
